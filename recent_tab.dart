import 'package:flutter/material.dart';

class RecentTab extends StatelessWidget {
  const RecentTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      // Use Center widget for centering
      child: Text('1st tab'),
    );
  }
}
